import {Button} from "react-bootstrap";
import React from "react";

export default function ModalSuccessButton(props) {
    return (
        <Button type={"button"} variant={"success"} {...props}/>
    );
}